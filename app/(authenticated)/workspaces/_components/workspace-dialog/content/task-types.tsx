import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function TaskTypesContent() {
    return (
      <Card>
        <CardHeader>
          <CardTitle>Task Types</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Task type configuration will go here.</p>
        </CardContent>
      </Card>
    );
  } 